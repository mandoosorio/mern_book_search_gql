// Step 9: create resolvers and typeDefs files, export them here in index
const typeDefs = require("./typeDefs");
const resolvers = require("./resolvers");

module.exports = { typeDefs, resolvers };

// Step 10: work on typeDefs first